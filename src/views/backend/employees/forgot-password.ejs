<div class="flex h-full w-full flex-grow items-center justify-center">
  <div class="w-120 rounded-2xl bg-white p-6 shadow-[0_2px_30px_rgba(0,0,0,.06)] lg:p-10">
    <div class="mb-2 flex items-center justify-center gap-1.5 py-4">
      <span class="text-slate-700">
        <img src="<%=settings.logo%>" alt="<%=settings.companyInfo.name%>" class="h-8 w-8" />
      </span>
      <span class="text-2xl font-bold text-slate-700"><%=settings.companyInfo.name.toUpperCase()%></span>
    </div>
    <div class="py-4 text-center">관리자에게 문의해 주세요.</div>
    <div class="p-4 text-center">
      <button
        class="focus:ring-opacity-50 rounded-md bg-slate-700 px-4 py-2 text-white hover:bg-slate-800 focus:ring-2 focus:ring-slate-600 focus:outline-none"
      >
        로그인 페이지로 돌아가기
      </button>
    </div>
  </div>
</div>

<script nonce="<%=nonce%>">
  document.addEventListener('DOMContentLoaded', () => {
    const button = document.querySelector('button');
    button.addEventListener('click', () => {
      window.history.back();
    });
  });
</script>
